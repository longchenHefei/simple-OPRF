diff --git a/emp-zk/emp-vole/spfss_recver.h b/emp-zk/emp-vole/spfss_recver.h
index e687dfd..d252398 100644
--- a/emp-zk/emp-vole/spfss_recver.h
+++ b/emp-zk/emp-vole/spfss_recver.h
@@ -1,4 +1,5 @@
 #ifndef SPFSS_RECVER_FP_H__
+#include <emp-tool/utils/twokeyprp.h>
 #define SPFSS_RECVER_FP_H__
 #include "emp-zk/emp-vole/utility.h"
 #include <emp-ot/emp-ot.h>
diff --git a/emp-zk/emp-vole/spfss_sender.h b/emp-zk/emp-vole/spfss_sender.h
index ccc1c32..2709841 100644
--- a/emp-zk/emp-vole/spfss_sender.h
+++ b/emp-zk/emp-vole/spfss_sender.h
@@ -1,4 +1,5 @@
 #ifndef SPFSS_SENDER_FP_H__
+#include <emp-tool/utils/twokeyprp.h>
 #define SPFSS_SENDER_FP_H__
 #include "emp-zk/emp-vole/utility.h"
 #include <emp-ot/emp-ot.h>
diff --git a/oprf/gmp-oprf.h b/oprf/gmp-oprf.h
index 90b94b6..b5bcfbe 100644
--- a/oprf/gmp-oprf.h
+++ b/oprf/gmp-oprf.h
@@ -1250,6 +1250,9 @@ public:
     std::cout << "communication (B): " << com_test(ios)-com_main << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl;         
 #endif
+    auto t_true_online = clock_start();
+    uint64_t emp_at_offline = com_test(ios)-com_main;
+    uint64_t ot_at_offline = sock.bytesSent();
 
     std::vector<uint8_t> ext(48 * sz);
     io->recv_data(&ext[0], 48 * sz);
@@ -1284,6 +1287,14 @@ public:
     io->send_data(&hex_pi[0], 96);
     io->flush();
     cur += sz;
+#ifdef ENABLE_FINEGRAIN
+    double true_online_us = time_from(t_true_online);
+    std::cout << "true_online_us: " << true_online_us << std::endl;
+    std::cout << "true_online_us_per_eval: " << (true_online_us / sz) << std::endl;
+    std::cout << "true_online_emp_B: " << (com_test(ios)-com_main - emp_at_offline) << std::endl;
+    std::cout << "true_online_emp_B_per_eval: " << ((com_test(ios)-com_main - emp_at_offline) / (double)sz) << std::endl;
+    std::cout << "true_online_libOT_sent_B: " << (sock.bytesSent() - ot_at_offline) << std::endl;
+#endif
   }  
 
   // single malicious
@@ -1516,6 +1527,9 @@ public:
     std::cout << "communication (B): " << com_test(ios)-com_main << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl; 
 #endif
+    auto t_true_online = clock_start();
+    uint64_t emp_at_offline = com_test(ios)-com_main;
+    uint64_t ot_at_offline = sock.bytesSent();
 
     std::vector<uint8_t> ext(48 * sz);
     y.resize(sz);
@@ -1564,6 +1578,14 @@ public:
     cout << "opening check pass" << endl;
 #endif
     cur += sz;
+#ifdef ENABLE_FINEGRAIN
+    double true_online_us = time_from(t_true_online);
+    std::cout << "true_online_us: " << true_online_us << std::endl;
+    std::cout << "true_online_us_per_eval: " << (true_online_us / sz) << std::endl;
+    std::cout << "true_online_emp_B: " << (com_test(ios)-com_main - emp_at_offline) << std::endl;
+    std::cout << "true_online_emp_B_per_eval: " << ((com_test(ios)-com_main - emp_at_offline) / (double)sz) << std::endl;
+    std::cout << "true_online_libOT_sent_B: " << (sock.bytesSent() - ot_at_offline) << std::endl;
+#endif
   }  
 
   // with libOTe
diff --git a/oprf/util/libot-pre.h b/oprf/util/libot-pre.h
index 3ddc957..c09558c 100644
--- a/oprf/util/libot-pre.h
+++ b/oprf/util/libot-pre.h
@@ -167,20 +167,21 @@ class LibOTPre { public:
 
 	void send_pre(block * data, block in_Delta) {
 		Delta = in_Delta;
-		ccrh.Hn(pre_data, data, n, pre_data+n);
+		// emp-tool CCRH::Hn(out, in, id, length[, scratch])
+		ccrh.Hn(pre_data, data, 0, n, pre_data+n);
 		xorBlocks_arr(pre_data+n, data, Delta, n);
-		ccrh.Hn(pre_data+n, pre_data+n, n);
+		ccrh.Hn(pre_data+n, pre_data+n, 0, n);
 	}
 
 	void recv_pre(block * data, bool * b) {
 		memcpy(bits, b, n);
-		ccrh.Hn(pre_data, data, n);
+		ccrh.Hn(pre_data, data, 0, n);
 	}
 
 	void recv_pre(block * data) {
 		for(int i = 0; i < n; ++i)
 			bits[i] = getLSB(data[i]);
-		ccrh.Hn(pre_data, data, n);
+		ccrh.Hn(pre_data, data, 0, n);
 	}
 
 	void choices_sender() {
diff --git a/test/oprf/test_malicious_oprf.cpp b/test/oprf/test_malicious_oprf.cpp
index 8c050a7..d1fabc7 100644
--- a/test/oprf/test_malicious_oprf.cpp
+++ b/test/oprf/test_malicious_oprf.cpp
@@ -1,10 +1,15 @@
+#define ENABLE_PQ
+#define ENABLE_FINEGRAIN
+#define ENABLE_SMALLN
 #define ENABLE_MALICIOUS
-//#define ENABLE_SMALLN // for small n testing
+#define ENABLE_SMALLN // for small n testing
 
 #include <gmpxx.h>
 #include <iostream>
 #include <cstdio>
 #include <cstdlib>
+#include <chrono>
+#include <thread>
 #include "oprf/oprf.h"
 #include "emp-tool/emp-tool.h"
 #include "oprf/gmp-oprf.h"
@@ -27,7 +32,16 @@ int main(int argc, char **argv) {
   if (party == ALICE) {
     sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), true);
   } else {
-    sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), false);
+    // NetIO connect returns before ALICE reaches asio listen; retry briefly.
+    for (int attempt = 0;; ++attempt) {
+      try {
+        sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), false);
+        break;
+      } catch (...) {
+        if (attempt >= 100) throw;
+        std::this_thread::sleep_for(std::chrono::milliseconds(50));
+      }
+    }
   }             
 
 
@@ -49,23 +63,35 @@ int main(int argc, char **argv) {
   com11 = comm2(ios);    
 
   // tmptmp
+  std::cout << "batch_n: " << test_nn << std::endl;
   if (party == ALICE) {
     GMP_PRG_FP prgdelta;
     mpz_class delta; // = prgdelta.sample();
 
     Oprf<BoolIO<NetIO>> oprf(party, threads, ios, sock);
 
-    auto start = clock_start();
-
+    auto t_all = clock_start();
+    auto t0 = clock_start();
     oprf.setup(delta, sock); 
     oprf.setup_malicious(sock);
-    //oprf.malicious_offline(100000);
-    //oprf.oprf_eval_server();   
+    double offline_us = time_from(t0);
+    uint64_t emp_off = comm(ios) - com1;
+    uint64_t ot_off = sock.bytesSent();
+    std::cout << "offline_us: " << offline_us << std::endl;
+    std::cout << "offline_emp_B: " << emp_off << std::endl;
+    std::cout << "offline_libOT_sent_B: " << ot_off << std::endl;
+
+    auto t1 = clock_start();
     oprf.oprf_batch_eval_server(test_nn, sock);
-
-    double ttt = time_from(start);
-    std::cout << "oprf eval: " << ttt << " us" << std::endl; 
+    double online_us = time_from(t1);
+    double ttt = time_from(t_all);
     uint64_t com2 = comm(ios) - com1;
+    std::cout << "online_us: " << online_us << std::endl;
+    std::cout << "online_us_per_eval: " << (online_us / test_nn) << std::endl;
+    std::cout << "online_emp_B: " << (com2 - emp_off) << std::endl;
+    std::cout << "online_emp_B_per_eval: " << ((com2 - emp_off) / (double)test_nn) << std::endl;
+    std::cout << "online_libOT_sent_B: " << (sock.bytesSent() - ot_off) << std::endl;
+    std::cout << "oprf eval: " << ttt << " us" << std::endl; 
     std::cout << "communication (B): " << com2 << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl; 
 
@@ -78,19 +104,31 @@ int main(int argc, char **argv) {
     Oprf<BoolIO<NetIO>> oprf(party, threads, ios, sock);
     mpz_class tmptmp;
 
-    auto start = clock_start();
-
+    auto t_all = clock_start();
+    auto t0 = clock_start();
     oprf.setup(tmptmp, sock);
     oprf.setup_malicious(sock); 
-    //oprf.malicious_offline(100000);
+    double offline_us = time_from(t0);
+    uint64_t emp_off = comm(ios) - com1;
+    uint64_t ot_off = sock.bytesSent();
+    std::cout << "offline_us: " << offline_us << std::endl;
+    std::cout << "offline_emp_B: " << emp_off << std::endl;
+    std::cout << "offline_libOT_sent_B: " << ot_off << std::endl;
+
     std::vector<mpz_class> in(test_nn);
     for (int i = 0; i < test_nn; i++) in[i] = i;
     std::vector<mpz_class> out(test_nn);
+    auto t1 = clock_start();
     oprf.oprf_batch_eval_client(&in[0], test_nn, out, sock);
-
-    double ttt = time_from(start);
-    std::cout << "oprf eval: " << ttt << " us" << std::endl;    
+    double online_us = time_from(t1);
+    double ttt = time_from(t_all);
     uint64_t com2 = comm(ios) - com1;
+    std::cout << "online_us: " << online_us << std::endl;
+    std::cout << "online_us_per_eval: " << (online_us / test_nn) << std::endl;
+    std::cout << "online_emp_B: " << (com2 - emp_off) << std::endl;
+    std::cout << "online_emp_B_per_eval: " << ((com2 - emp_off) / (double)test_nn) << std::endl;
+    std::cout << "online_libOT_sent_B: " << (sock.bytesSent() - ot_off) << std::endl;
+    std::cout << "oprf eval: " << ttt << " us" << std::endl;    
     std::cout << "communication (B): " << com2 << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl; 
 
diff --git a/test/oprf/test_single_malicious_oprf.cpp b/test/oprf/test_single_malicious_oprf.cpp
index c1ec07c..27e9728 100644
--- a/test/oprf/test_single_malicious_oprf.cpp
+++ b/test/oprf/test_single_malicious_oprf.cpp
@@ -1,3 +1,5 @@
+#define ENABLE_PQ
+#define ENABLE_FINEGRAIN
 #define ENABLE_MALICIOUS // we have to do this to ahieve half-malicious
 #define ENABLE_SS // enable SS VOLE
 
@@ -5,6 +7,8 @@
 #include <iostream>
 #include <cstdio>
 #include <cstdlib>
+#include <chrono>
+#include <thread>
 #include "oprf/oprf.h"
 #include "emp-tool/emp-tool.h"
 #include "oprf/gmp-oprf.h"
@@ -27,7 +31,16 @@ int main(int argc, char **argv) {
   if (party == ALICE) {
     sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), true);
   } else {
-    sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), false);
+    // NetIO connect returns before ALICE reaches asio listen; retry briefly.
+    for (int attempt = 0;; ++attempt) {
+      try {
+        sock = osuCrypto::cp::asioConnect(string(argv[3])+":"+string(argv[2]), false);
+        break;
+      } catch (...) {
+        if (attempt >= 100) throw;
+        std::this_thread::sleep_for(std::chrono::milliseconds(50));
+      }
+    }
   }     
 
   std::cout << std::endl
@@ -50,24 +63,32 @@ int main(int argc, char **argv) {
 
     Oprf<BoolIO<NetIO>> oprf(party, threads, ios, sock);
 
-    auto start = clock_start();
-
+    auto t_all = clock_start();
+    auto t0 = clock_start();
 #ifdef ENABLE_SS
     oprf.setup_base(delta, sock, true); 
 #else
     oprf.setup_base(delta, sock); 
 #endif
-
-    //oprf.oprf_eval_server();   
     oprf.setup_malicious_base(sock);
+    double offline_us = time_from(t0);
+    uint64_t emp_off = comm(ios) - com1;
+    uint64_t ot_off = sock.bytesSent();
+    std::cout << "offline_us: " << offline_us << std::endl;
+    std::cout << "offline_emp_B: " << emp_off << std::endl;
+    std::cout << "offline_libOT_sent_B: " << ot_off << std::endl;
+
+    auto t1 = clock_start();
     oprf.oprf_batch_eval_server_base(test_nn, sock);
-
-    double ttt = time_from(start);
-    std::cout << "oprf eval: " << ttt << " us" << std::endl; 
+    double online_us = time_from(t1);
+    double ttt = time_from(t_all);
     uint64_t com2 = comm(ios) - com1;
+    std::cout << "online_us: " << online_us << std::endl;
+    std::cout << "online_emp_B: " << (com2 - emp_off) << std::endl;
+    std::cout << "online_libOT_sent_B: " << (sock.bytesSent() - ot_off) << std::endl;
+    std::cout << "oprf eval: " << ttt << " us" << std::endl; 
     std::cout << "communication (B): " << com2 << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl; 
-    //std::cout << "comm. libOT (B): " << sock.bytesReceived()+sock.bytesSent() << std::endl; 
 
     std::cout << "correctness checking..." << std::endl;
     std::vector<uint8_t> ext(48);
@@ -78,26 +99,35 @@ int main(int argc, char **argv) {
     mpz_class tmptmp;
     Oprf<BoolIO<NetIO>> oprf(party, threads, ios, sock);
 
-    auto start = clock_start();
-
+    auto t_all = clock_start();
+    auto t0 = clock_start();
 #ifdef ENABLE_SS
     oprf.setup_base(tmptmp, sock, true);
 #else
     oprf.setup_base(tmptmp, sock);
 #endif
-
     oprf.setup_malicious_base(sock);
+    double offline_us = time_from(t0);
+    uint64_t emp_off = comm(ios) - com1;
+    uint64_t ot_off = sock.bytesSent();
+    std::cout << "offline_us: " << offline_us << std::endl;
+    std::cout << "offline_emp_B: " << emp_off << std::endl;
+    std::cout << "offline_libOT_sent_B: " << ot_off << std::endl;
+
     std::vector<mpz_class> in(test_nn);
     for (int i = 0; i < test_nn; i++) in[i] = i;
     std::vector<mpz_class> out(test_nn);
+    auto t1 = clock_start();
     oprf.oprf_batch_eval_client_base(&in[0], test_nn, out, sock);
-
-    double ttt = time_from(start);
-    std::cout << "oprf eval: " << ttt << " us" << std::endl;    
+    double online_us = time_from(t1);
+    double ttt = time_from(t_all);
     uint64_t com2 = comm(ios) - com1;
+    std::cout << "online_us: " << online_us << std::endl;
+    std::cout << "online_emp_B: " << (com2 - emp_off) << std::endl;
+    std::cout << "online_libOT_sent_B: " << (sock.bytesSent() - ot_off) << std::endl;
+    std::cout << "oprf eval: " << ttt << " us" << std::endl;    
     std::cout << "communication (B): " << com2 << std::endl;
     std::cout << "comm. libOT (B): " << sock.bytesSent() << std::endl; 
-    //std::cout << "comm. libOT (B): " << sock.bytesReceived()+sock.bytesSent() << std::endl; 
 
     std::cout << "correctness checking..." << std::endl;
     std::vector<uint8_t> ext(48);
